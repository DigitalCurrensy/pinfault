# Waves

W0: two published pins (Apollo 11 LM, IM-1 Odysseus) vs declared LOLA/SLDEM tile stats. Scorer named, not run.

W1: void + slope scorer. `score.py` ported. Voids first. Both published pins fail offset.

W2: one bad pin demonstration. PIN-A11-LM walked. West crater 400 m. Pretty-print is not a pass.

W3: memo template. Paper on the pin. Fail still issues. Apollo 12 named, not this memo. `void_fraction = n_invalid / n_cells`.

W4: surveyor pass. Locked. Do not claim survey-grade without a surveyor.

Stop. Do not staff RIMKEEP.
